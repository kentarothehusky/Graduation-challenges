<div class="jumbotron">
  <div class="container">
    <div class="introduction">

      <h1 class="h1_user_new">あなたのミッションはその資料を作ることですか？<h1>
      <h4>資料は"作る"ものではなく"売買する"時代に。 <br>
          ProposalStationは、提案資料のセカンドマーケットプレイスです。</h4>
    
      <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@user.errors.count, "error") %> prohibited this @user from being saved:</h2>
          <ul>
          <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
    </div>  
    
      <div class="form_new">  
        <h2>新規登録フォーム</h2>
        <%= form_with(model: @user, local: true) do |f| %>  
          <p class="form"><%= f.label :お名前 %></p>
          <%= f.text_field :name %>
        
          <p class="form"><%= f.label :Eメールアドレス %></p>
          <%= f.email_field :email %>
        
          <p class="form"><%= f.label :パスワード %></p>
          <%= f.password_field :password %>
        
          <p class="form"><%= f.label :確認用パスワード %></p>
          <%= f.password_field :password_confirmation %>
          <br>
          <br>
        
          <%= f.submit "ユーザー登録" %>
        <% end %>
      </div>
    </div>
</div>