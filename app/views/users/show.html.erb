<h1><%= @user.name %>さんのページ</h1>

<h2><%= @user.name %>さんの投稿一覧</h2>

<% @contributions.each do |ontribution| %> 
  <td><%= ontribution.title %></td>
  <td><%= ontribution.overview %></td>
  <td><%= ontribution.modified %></td>
<% end %>
