<!DOCTYPE html>
<html>
  <head>
    <title>Proposal</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div id="gnavi" class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <% if logged_in? %>
          <li><%= link_to_unless_current "資料を探す", contributions_path %></li>
          <li><%= link_to "投稿してみる", new_contribution_path%></li>
        <!--ココにfavoriteを追加-->
          <li><%= link_to_unless_current "マイページ", users_path %></li>
          <li><%= link_to_unless_current "ログアウト", session_path(current_user.id), method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign up", new_user_path %></li>
          <li><%= link_to "Login", new_session_path %></li>
        <% end %>
              
        <% flash.each do |key, value| %>
          <%= content_tag(:div, value, class: "#{key}") %>
        <% end %>
      </ul>
    </div>
    <%= yield %>

  </body>
</html>
